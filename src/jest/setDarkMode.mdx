import {Meta} from "@storybook/blocks";

<Meta title="jest/setDarkMode"/>

# setDarkMode

Simple util for setting color scheme to `light` or `dark`

```typescript
describe('Color Scheme Suite', () => {
    test('Light Mode', async () => {
        setDarkMode(false);

        expect(window.matchMedia('(prefers-color-scheme: light)').matches).toBe(true);
        expect(window.matchMedia('(prefers-color-scheme: dark)').matches).toBe(false);
    });

    test('Dark Mode', async () => {
        setDarkMode(true);

        expect(window.matchMedia('(prefers-color-scheme: light)').matches).toBe(false);
        expect(window.matchMedia('(prefers-color-scheme: dark)').matches).toBe(true);
    });
});
```