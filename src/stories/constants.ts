import {ArgTypes} from '@storybook/react';

export const DisabledArgType: ArgTypes = {table: {disable: true}};
export const MuiColorArgType: ArgTypes = {
	options: ['secondary', 'primary', 'inherit']
};